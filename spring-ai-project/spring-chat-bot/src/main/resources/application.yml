spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ai_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&allowMultiQueries=true
    username: zmbdp
    password: 123456
  application:
    name: spring-chat-bot
  ai:
#    openai:
#      api-key: ${DEEPSEEK_API_KEY}
#      base-url: https://api.deepseek.com
#      chat:
#        options:
#          model: deepseek-chat
#          temperature: 0.7
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-vl-max-latest
          multi-model: true
logging:
  level:
    org.springframework.ai.chat.client.advisor: debug
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
